"$schema" = "https://yazi-rs.github.io/schemas/yazi.json"

[mgr]
ratio          = [2, 4, 3]
sort_by        = "alphabetical"
sort_sensitive = false
sort_reverse   = false
sort_dir_first = true
sort_translit  = false
linemode       = "none"
show_hidden    = true
show_symlink   = true
scrolloff      = 5
mouse_events   = ["click", "scroll"]
title_format   = "Yazi: {cwd}"

[preview]
wrap            = "no"
tab_size        = 2
max_width       = 1200
max_height      = 800
cache_dir       = ""
image_delay     = 30
image_filter    = "triangle"
image_quality   = 75
sixel_fraction  = 15
ueberzug_scale  = 0.85
ueberzug_offset = [25, 1, -0.5, -0.5]

[opener]
edit = [
    { run = '${EDITOR:-nvim} "$@"', desc = "$EDITOR", block = true, for = "unix" },
    { run = 'code %*',    orphan = true, desc = "code", for = "windows" },
    { run = 'code -w %*', block = true, desc = "code (block)", for = "windows" },
]
open = [
    { run = 'xdg-open "$1"', desc = "Open", for = "linux" },
    { run = 'open "$@"', desc = "Open", for = "macos" },
    { run = 'start "" "%1"', orphan = true, desc = "Open", for = "windows" },
    { run = 'termux-open "$1"', desc = "Open", for = "android" },
]
reveal = [
    { run = 'xdg-open "$(dirname "$1")"', desc = "Reveal", for = "linux" },
    { run = 'open -R "$1"', desc = "Reveal", for = "macos" },
    { run = 'explorer /select,"%1"', orphan = true, desc = "Reveal", for = "windows" },
    { run = 'termux-open "$(dirname "$1")"', desc = "Reveal", for = "android" },
]
extract = [
    { run = '7z x "$@"', desc = "Extract here", for = "unix" },
]
play = [
    { run = 'mpv --force-window "$@"', orphan = true, for = "unix" },
]

[open]
rules = [
    { name = "*/", use = ["open", "reveal"] },
    { mime = "text/*", use = ["preview", "edit", "reveal"] },
    { mime = "image/*", use = ["preview", "open", "reveal"] },
    { mime = "{audio,video}/*", use = ["play", "reveal"] },
    { mime = "application/{zip,rar,7z*,tar,gzip,xz,zstd,bzip*,lzma,compress,archive,cpio,arj,xar,ms-cab*}", use = ["extract", "reveal"] },
    { mime = "application/{json,ndjson}", use = ["preview", "edit", "reveal"] },
    { mime = "*/javascript", use = ["preview", "edit", "reveal"] },
    { mime = "inode/empty", use = ["edit", "reveal"] },
    { name = "*", use = ["open", "reveal"] },
]

[input]
cursor_blink = false

[confirm]
trash_title  = "Trash {n} selected file{s}?"
delete_title = "Permanently delete {n} selected file{s}?"
quit_title   = "Quit?"

[pick]
open_title = "Open with:"

