return {
  "yetone/avante.nvim",
  opts = {
    provider = "ollama",
    providers = {
      ollama = {
        endpoint = "http://127.0.0.1:11434",
        model = "qwen2.5-coder:1.5b",
        disable_tools = true,
        -- Use the internal defaults for parsing to save CPU cycles
      },
    },
    -- VERY IMPORTANT: Reduce the amount of data sent to the Pi
    hints = { enabled = false },
    behaviour = {
      auto_suggestions = false,
      -- This stops Avante from reading your whole project tree
      enable_cursor_planning_mode = false,
    },
  },
}
